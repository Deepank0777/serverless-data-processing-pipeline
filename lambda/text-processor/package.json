{
  "name": "text-processor",
  "version": "1.0.0",
  "description": "Lambda function for processing text files",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1130.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.97",
    "@types/node": "^17.0.31",
    "@types/uuid": "^8.3.4",
    "typescript": "^4.6.4"
  }
}
